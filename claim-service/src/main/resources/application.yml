server:
  port: 4005

spring:
  application:
    name: claim-service

  data:
    mongodb:
      username: ""
      uri: mongodb://localhost:27017/legato-backend


eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
#      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
      defaultZone: http://localhost:8761/eureka/